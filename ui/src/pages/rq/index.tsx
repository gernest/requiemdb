import styled from 'styled-components';
import { Layout } from '../layout';


const Wrapper = styled.div`
`

export const Main = () => {
    return (
        <Wrapper>
            <Layout />
        </Wrapper>
    )
}